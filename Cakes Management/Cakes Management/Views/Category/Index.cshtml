@model List<Category>
@{
    ViewData["Title"] = "Category";
}

<table class="table table-bordered table-hover table-striped">
    <thead class="thead-dark">
        <tr>
            <th>#ID</th>
            <th>Category name</th>
            <th>Products</th>
        </tr>
    </thead>
    <tbody>
        @foreach (Category cat in Model)
        {
            <tr>
                <td>@cat.CategoryId</td>
                <td> @cat.CategoryName</td>
                <td class="text-center"><a title="show products" class="btn btn-outline-info btn-sm" asp-controller="Cake" asp-action="Index" asp-route-catId="@cat.CategoryId">Danh sách bánh</a></td>
            </tr>
        }
    </tbody>
</table>
